# Anthropic API key (required for LLM-powered pipeline steps)
ANTHROPIC_API_KEY=sk-ant-...

# Google Drive service account credentials (optional — for --source gdrive)
# Either set this env var to the JSON string, or place a credentials.json file in the project root.
GOOGLE_SERVICE_ACCOUNT_JSON=
